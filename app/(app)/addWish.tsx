import { Text } from "@/components/ui";
import { SafeAreaView } from "react-native";

export default function addWish() {
  return (
    <SafeAreaView className="flex-1 bg-[#1e1f35] items-center justify-center">
      <Text>Add Wish</Text>
    </SafeAreaView>
  );
}
